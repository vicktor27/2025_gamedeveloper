const Footer = (props) => {
    return(
        <>
            <footer>
                <p>Este es el footer con drop drilling : {props.count}</p>
            </footer>
        </>

    );
}

export default Footer;